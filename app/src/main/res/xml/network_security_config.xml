<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 
      Universal Base Config:
      Permits cleartext HTTP traffic dynamically for ALL external creator vibe-sites.
      Zero hardcoded creator domain names — any creator site works automatically!
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Strict HTTPS enforcement for primary app domain -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">music-wtf.vercel.app</domain>
        <domain includeSubdomains="true">vercel.app</domain>
    </domain-config>
</network-security-config>
