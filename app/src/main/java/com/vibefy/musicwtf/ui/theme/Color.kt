package com.vibefy.musicwtf.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Palette (from DESIGN_TOKENS.md) ─────────────────────────
val Night        = Color(0xFFEEF6FC)   // Soft sky background
val Night2       = Color(0xFFFFFFFF)
val Night3       = Color(0xFFCFE3F2)
val Moon         = Color(0xFF12212E)   // Primary text / headings
val MoonDim      = Color(0xFF4D6578)   // Secondary text
val Lamp         = Color(0xFF2489D3)   // Primary accent – cerulean blue
val LampSoft     = Color(0x242489D3)   // 14% lamp tint

// ── Dark Player Palette ────────────────────────────────────────────
val DeckDark     = Color(0xFF14171E)   // Drawer background
val DeckDeep     = Color(0xFF0D0F14)
val ScreenBlack  = Color(0xFF05070F)   // TV screen / CRT
val PageBg       = Color(0xFF07080C)   // Full-screen page background

// ── Accent / Status ────────────────────────────────────────────────
val PowerGreen   = Color(0xFF10B981)   // Live dot / power LED
val Amber        = Color(0xFFF59E0B)   // WTF! splash + tagline + LED alt
val SkyBlue      = Color(0xFF38BDF8)   // Cassette side label
val OfflineRed   = Color(0xFFEF4444)
val TruckRed     = Color(0xFFD62828)   // Hero text shadow

// ── Active Chip Gradient endpoints ────────────────────────────────
val ChipActiveStart = Color(0xFF3EA2E8)
val ChipActiveEnd   = Color(0xFF1F7FC7)

// ── Player card glass ──────────────────────────────────────────────
val GlassWhite4  = Color(0x0AFFFFFF)   //  4% white
val GlassWhite8  = Color(0x14FFFFFF)   //  8% white
val GlassWhite12 = Color(0x1EFFFFFF)   // 12% white
